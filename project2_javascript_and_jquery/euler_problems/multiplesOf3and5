var multiplesof3and5 = {
    var total = 0;
    var getTotal = function() {
        for (i = 1; i < 1000; i++) {
            if (i % 3 === 0 || i % 5 === 0) {
                 total += i;
        }
    }
  return total;
};

getTotal();
